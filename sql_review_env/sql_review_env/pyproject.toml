[project]
name = "openenv-sql-review-env"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = ["openenv-core[core]>=0.2.2"]
[project.scripts]
server = "sql_review_env.server.app:main"